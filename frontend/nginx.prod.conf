# Used when FRONTEND_TARGET=production (frontend serves its own built files)
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
